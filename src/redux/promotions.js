import { PROMOTIONS } from '../shared/promotions';

export const Promotions = (state = PROMOTIONS, action) => { //Reducer function. 
    switch(action.type) {
        default:
            return state;
    }
}